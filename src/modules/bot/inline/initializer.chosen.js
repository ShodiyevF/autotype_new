const chosenInitializer = (bot, res) => {
    try {
        console.log('asdasd');
        console.log(res);
    } catch (error) {
        console.log(error.message)
    }
}

module.exports = {
    chosenInitializer
}